# 《人生分"枝"》服务器运维手册（无 AI 也能操作）

> 面向：你自己或队友日常维护。目标服务器：阿里云国际 ECS/轻量 `47.79.79.41`（Ubuntu 24.04）。
> 服务形态：Node `standalone-server.mjs` + systemd 常驻，代码托管在 GitHub。

---

## 0. 常用入口与凭证速查

| 项 | 值 |
|---|---|
| 公网首页 | http://47.79.79.41:8080/ |
| 数据后台 | http://47.79.79.41:8080/admin |
| SSH | `ssh -i Arcyuki.pem root@47.79.79.41`（私钥在本地项目目录） |
| 服务器代码目录 | `/root/life-branches`（Git 仓库，分支 `fix/zhihu-cli-windows-path`） |
| 运行目录 | `/root/life-branches/prototype` |
| 环境变量文件 | `/etc/life-branches.env`（含 Access Secret 与 ADMIN_PASSWORD，仅 root 可读） |
| 系统服务 | `life-branches`（systemd，开机自启） |
| 数据文件 | `/root/life-branches/prototype/.local/feedback.jsonl`、`usage.jsonl`、`search-cache.jsonl` |

## 1. 日常检查（服务还活着吗）

```bash
ssh -i Arcyuki.pem root@47.79.79.41
systemctl status life-branches          # active = 正常
journalctl -u life-branches -n 50       # 看最近日志
curl -s http://127.0.0.1:8080/api/branches/health   # 本机探活
```

浏览器直接开 http://47.79.79.41:8080/ 和 /admin 也应正常。

**如果服务挂了/重启：**
```bash
systemctl restart life-branches
systemctl enable life-branches   # 确保开机自启（已配置，一般不用重复）
```

## 2. 改管理员密码 / 环境变量

```bash
vi /etc/life-branches.env          # 改 ADMIN_PASSWORD= 或其它变量
systemctl restart life-branches
```
改完用新密码登录 http://47.79.79.41:8080/admin 验证。
（`ZHIHU_ACCESS_SECRET` 是知乎服务端 Secret，别外泄；`PORT=8080` 别动，除非同步改安全组。）

## 3. 代码更新：把 GitHub 最新内容同步到服务器

前提：代码已 push 到 GitHub 的 `fix/zhihu-cli-windows-path`（在你电脑：`git push origin fix/zhihu-cli-windows-path`）。

### 情形 A：只改了后端 `.mjs` / 源码（不需要重新构建前端）
```bash
cd /root/life-branches
git fetch origin
git reset --hard origin/fix/zhihu-cli-windows-path   # 服务器代码=GitHub
systemctl restart life-branches
```

### 情形 B：改了前端页面（workspace.tsx / 样式等）→ 必须重新构建并上传 dist

因为服务器内存小，**不在服务器上 npm ci / build**，改在**你自己电脑**上构建再传：

```powershell
# —— 在你的电脑（Windows）——
cd C:\Users\24176\Desktop\hacksonProject\life-branches\prototype
npm run build                        # 生成 dist/
tar -czf dist.tgz dist               # 打包
scp -i C:\Users\24176\Desktop\hacksonProject\Arcyuki.pem dist.tgz root@47.79.79.41:/root/dist.tgz
```
```bash
# —— 再 ssh 到服务器 ——
cd /root/life-branches
git fetch origin
git reset --hard origin/fix/zhihu-cli-windows-path
cd prototype
tar -xzf /root/dist.tgz              # 覆盖 dist
systemctl restart life-branches
curl -s http://127.0.0.1:8080/api/branches/health   # 验证
```

> 提示：先 `git push` 再 `git reset --hard` 拉取，保证服务器代码与 GitHub 一致。

## 4. 为什么服务器不能直接 npm install（背景）

服务器可用内存只有 ~400MB，`npm ci`（几 GB 依赖）会被系统 OOM 杀掉。
所以本项目采用**免安装部署**：依赖只在开发机安装、`npm run build` 产出平台无关的 `dist`，
服务器只跑 Node 内置 + dist。**不要**在服务器上执行 `npm ci / npm install / npm run build`。

## 5. 备份与恢复数据

```bash
tar -czf /root/backup-$(date +%F).tgz /root/life-branches/prototype/.local
# 恢复：把备份解压回 .local 目录即可
```
建议定期备份 `.local`（反馈/用量/缓存）。

## 6. 常见问题

| 现象 | 处理 |
|---|---|
| 首页偶发 502 / 慢 | 低内存 + swap 冷启动导致，稍后重试或 `systemctl restart life-branches` |
| 服务器 ssh 连不上（banner 超时） | 常因负载高；等几分钟再试；仍不行去阿里云控制台 Workbench 或重启实例 |
| /admin 登录不上 | 检查 `/etc/life-branches.env` 里 ADMIN_PASSWORD 是否被改过 |
| 知乎额度耗尽 | 登录知乎开放平台个人中心查看/申请；额度每日自然日刷新 |
| 私钥权限报错 | Windows：`icacls Arcyuki.pem /inheritance:r` 后 `icacls Arcyuki.pem /grant:r "$env:USERNAME:(R)"` |

## 7. 换一台服务器/重装（完整重来）

1. 买新实例（境外免备案；Ubuntu 24.04；至少 2G 内存更稳）
2. 本机执行 `ssh-keygen` 生成密钥并把公钥放服务器（或用厂商密钥对）
3. 服务器装 Node：`curl -fsSL https://nodejs.org/dist/latest-v24.x/ | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | head -1` 下载对应 tar.xz 解压到 /usr/local
4. `git clone -b fix/zhihu-cli-windows-path https://github.com/iiForget/life-branches.git /root/life-branches`
5. 按上面"情形 B"上传 dist；写 `/etc/life-branches.env`；套用本手册里的 systemd 单元文件启动
6. 阿里云控制台放行 8080

> 本项目完整 systemd 单元见部署报告附录（若需完整命令可参考第 3 节与部署历史）。
