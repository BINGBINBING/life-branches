# 协作开发

## 工作方式

1. 用 Issue 说明问题、验收标准和涉及的页面或接口。
2. 从最新 `main` 建立 `feat/主题` 或 `fix/主题` 分支，每次只处理一个问题。
3. 提交 Pull Request，描述变更、验证结果和已知风险。
4. 经协作者审阅并通过自动检查后合并。当前是协作约定，不代表已配置分支保护。

## 验证要求

在 `prototype` 目录执行 `npm ci`，然后执行 `npm test`、`npm run lint`、`npx tsc --noEmit`、`npm run build`。

修改证据提取或匹配规则时，增加离线回归测试。引用文本存在不代表推断正确；需检查结论是否受到对应引文支持。修改界面时，说明桌面和手机布局、空状态和错误状态的验证情况。

真实验证使用虚构人物，并事先确认个人知乎额度。不要在自动检查中调用真实知乎接口。

## 数据与凭证

- 每位开发者使用自己的知乎凭证，不通过仓库、Issue 或聊天共享密钥。
- 不提交 `.env*`、`.local/`、系统凭证、真实用户画像、临时隧道记录或原始调用日志。
- 新增演示片段应保持最小必要引用、标明原文来源，避免整篇搬运。
- 泄露凭证后应立即撤销或轮换，删除文件本身不能清除 Git 历史中的秘密。

## 运行边界

实时后端仅在本地开发模式使用系统 CLI。临时公网展示只提供固定历史样本，不要直接将持有凭证的开发服务暴露到公网。
