# 人生分枝：原型开发规格

状态：用户已完成最终共同理解确认。原型已实现并在本地运行；历史样本可体验，实时全链路因知乎搜索额度耗尽尚未完成验收。

## 已确认决策

1. 聚焦转行、升学、专业选择、出国等人生选择。
2. 输出正反经验对照、关键差异、可参考做法、相关风险与还需确认的条件，辅助用户判断。
3. 搜索前少量提问，搜索中依据案例关键条件动态补问；允许跳过，不重复询问已有答案。
4. 一次只研究一个选择。
5. 使用分枝导航与经验详情。
6. 分枝按行动路径划分，从实际检索证据中提取；每条路径内部容纳成功与受挫经历。
7. 接入真实知乎搜索，跑通提问、检索、动态补问、分枝导航、来源查看；条件在应用内填写，首版不接知乎账号登录。

## 核心流程

用户输入选择 → 提取已知条件 → 少量关键追问 → 正反检索 → 筛选与聚合经验 → 按行动路径形成分枝 → 查看路径内的经验详情 → 根据影响适用性的未知条件补问 → 更新匹配解释。

以非科班转行开发为例，候选路径可能包括在职自学、脱产自学、培训，但仅在搜索证据支持时展示。分枝不是系统预测的人生结局。一个案例可以包含多个阶段结果，不强行把整篇文章划为成功或失败。

## 界面草案与默认处理

以下是根据已确认决策提出的实施默认值，并非用户另行指定。

- 首屏直接输入要研究的选择，不制作营销落地页。
- 上方展示可修改的用户条件，主区域以行动路径导航切换经验详情；移动端采用路径选择与纵向详情。
- 经验详情包含来源类型、背景、行动、阶段结果、与用户的相似点和差异、未知信息及对应证据。
- 路径下汇总可参考做法、相关风险、还需确认的条件，并链接支撑案例。
- 动态补问解释其与哪条经历相关；补答后优先重新分析已有证据，仅在需要新证据时补充搜索。
- 正反案例不强求数量相等。只有单侧证据时如实展示，不补造另一侧。
- 原文条件未知时标记来源未说明，不向用户询问作者的个人信息。

## 证据与技术边界

- 知乎检索由后端调用；凭证不进入浏览器、源代码或日志。
- 搜索摘要不是全文，个人自述不代表已独立核实。
- 去重时保留同一来源在不同查询下返回的不同片段。
- 区分个人自述、转述、建议、机构或推广内容；区分目标达成、阶段受挫、结果未明。
- 条件匹配使用明确相似、明确不同、未知，不显示未经验证的精确匹配分数或成功率。
- 空结果、超时、限流、分析失败均展示真实状态，不静默替换成虚构案例。
- 文本分析能力的模型接入在开发前检查现有环境；如缺少模型凭证，将明确报告依赖，不把关键词规则包装成已完成的语义分析。
- 暂不包含多人协作、知乎登录、长期自动追踪和后验验证。

## 验收标准

1. 一个真实选择可以走完提问、搜索、行动路径导航、经验详情及原文查看。
2. 每条重要适用性解释都可追溯到具体来源片段。
3. 将每日可投入时间等关键条件修改后，相关差异提示随之变化；没有证据时显示未知。
4. 案例结果混合、只有单侧证据和无结果时，仍有完整可理解的界面。
5. 动态补问不重复已有信息，不询问来源作者的未知条件。
6. 桌面与手机尺寸可用，长文本不遮挡操作，搜索和补问状态清楚。

实现完成后启动本地服务并提供访问地址。真实搜索与模型分析分别验证，报告仍未完成的依赖或检查。
