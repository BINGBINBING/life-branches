# DS JSON 格式错误调查与修复报告

日期：2026-09-10

## 事件与证据

本地“机械工程转计算机”研究（ea6defb7-602e-40b0-8966-f21553766f73）在取得48个来源后报“DeepSeek 未返回合法 JSON”。记录显示5次知乎搜索正常，中途分析检查点成功，最终分析阶段失败。失败时间为2026-09-10 14:43:13 UTC。

此提示来自 deepseekJSON 对响应正文执行 JSON 解析的异常分支。此前已通过 HTTP 成功状态、外层响应 JSON 解析及 finish_reason=stop 检查。因此证据指向模型消息正文的格式不符合本地解析要求，而不是知乎检索失败、鉴权失败或接口明确报告的输出截断。

原实现没有保存失败正文、解析位置或响应长度，不能追溯究竟是引号、逗号、代码围栏还是其他格式问题；也不能完全排除内容不完整但服务报告正常结束。复杂输出结构可能增加风险，但没有证据将其认定为本次唯一根因。

## 已修复

1. 安全规范化：仅移除外围空白、BOM或完整JSON代码围栏，不截取任意大括号，也不猜补原始事实。
2. 有界格式修复：仅最终主分析允许一次额外DS格式修复。中途检查点、低召回补查和审核不启动修复；失败不递归。鉴权、余额、网络及明确截断不触发格式修复。
3. 修复后仍执行原有对象结构、引用、数字及语义复核。模型修复不是事实核验，不保证内容完全不变；因此不能绕过后续校验。
4. 安全诊断：在忽略目录 .local/deepseek-format-errors.jsonl 记录时间、正文类型/长度、结束原因、错误位置、是否修复阶段。不记录提示词、正文、密钥或可能包含正文的原始异常消息。
5. 调用记账：成功修复合并两次用量并标记 extraCalls=1。最大分析预算声明改为7次，包含主分析、两类审核、最多两次低召回补查、一个检索检查点及一次格式修复；通常无需用满。失败调用费用仍可能发生，当前业务用量记录未覆盖所有失败响应的精确token数。
6. 本次48个来源已另存于本地忽略目录；未重新搜索知乎。已重启后台服务加载修复，避免仅更新前端而后端仍执行旧代码。

## 验证与边界

223项自动测试通过，含外围格式处理、畸形JSON拒绝、单次修复、用量合并、敏感信息不进入诊断、修复失败不循环、截断不盲修复。代码检查通过。

未声称复现原始损坏字节，因为原响应未留存。真实来源重放用于验证当前链路；一次成功重放也不代表已彻底消除模型偶发格式错误。拆分路径与辅助案例生成属于后续结构优化，本次没有同时重构整个分析流程。

## 真实重放结果

复用48个原来源调用本地重分析接口，再次发生正常结束但JSON解析失败：正文长度15868字符、解析位置501、finish_reason=stop。未保存正文，不能据此还原具体错误字符。

一次模型格式修复实际触发并成功，随后通过引用和语义审核；接口返回200、研究状态done，26个来源贡献58条分类依据。整轮6次DS调用（含格式修复及补查），新增知乎搜索0次。结果已保存成本地研究版本。类型检查、构建和差异格式检查均通过。
