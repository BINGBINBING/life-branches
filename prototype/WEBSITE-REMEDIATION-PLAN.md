# 人生分枝网站问题汇总与整改计划

> 创建日期：2026-09-08
> 适用范围：转专业、跨专业路径、转行业 / 转岗原型
> 目的：汇总两轮产品检查发现的全部问题，作为后续修改、验收和进度更新的统一依据。

## 一、处理原则

1. 先修会造成错误判断的数据语义和证据问题，再扩展路径和交互。
2. AI 负责提取、归纳和生成候选内容；本地规则负责字段范围、量纲、引用、阶段和输出上限。
3. 总结与原文必须分离：默认展示归纳，原文只作为可核对证据。
4. 搜索前条件来自用户；搜索后问题必须区分“新证据触发”和“基础信息缺失”。
5. 页面中的样本数、频次和阶段结果不得表述为成功率、因果关系或普遍规律。

## 二、优先级定义

| 优先级 | 含义 |
| --- | --- |
| P0 | 会造成错误结论、敏感信息风险或公网额度风险，必须优先修复 |
| P1 | 影响核心流程完整性、解释性或用户判断效率 |
| P2 | 影响易用性、可维护性和长期运营，但不直接改变结论 |

## 三、问题总览

### A. 决策类型与行动分枝

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| A01 | P0 | “校内转专业”同时承担决策类型和行动路径，所有案例被强制放进一个节点 | 分离 `decisionRoute` 与 `actionBranch` 两层数据结构 | 选择校内转专业后可出现多个有案例证据的行动分枝 |
| A02 | P1 | 转专业没有按实际做法导航 | 建立成绩与资格、先修补齐、考核准备、申请材料、转入后适应、失败后替代路径等受控分枝 | 每个分枝名称是行动方式，不是成功/失败结果；无法归类时进入“行动方式待确认” |
| A03 | P1 | 一个案例可能包含多个连续行动，当前只能归入单一路径 | 支持“主分枝 + 次要行动标签”，避免复制案例 | 同一案例只展示一次，同时保留其余明确行动标签 |
| A04 | P1 | 用户无法直接修正系统识别的校内转专业、跨专业读研、辅修等类型 | 在初始化表单增加决策类型确认控件 | 修改类型后字段目录和搜索计划同步更新，不要求用户重写描述 |

### B. AI 总结、原文与内容性质

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| B01 | P0 | 背景、行动、结果的 `text` 被本地校验逻辑直接替换成 `quote` | 保留 AI summary 和原文 quote 两个独立字段 | 默认显示简洁总结；展开后显示连续原文，两者不再相同 |
| B02 | P0 | “可参考做法”和“相关风险”主要是原文摘抄 | 要求 AI 输出“归纳 + 与用户的关系 + 证据”，本地只保留有引用支持的内容 | 每条结论回答做法/风险是什么、为何相关、有什么限制，并能跳转原文 |
| B03 | P0 | 态度或感受可能被识别为行动，例如“做了重要决定、从未后悔” | 增加行动语义校验，要求可观察的行为、步骤或投入 | 纯观点、情绪、意向不能进入“采取的行动” |
| B04 | P0 | 建议性内容可能被当成人生案例 | 恢复 `self / retold / advice / promotion` 内容性质分类并展示置信边界 | 亲历、转述、一般建议和推广内容可区分；建议不能冒充亲历结果 |
| B05 | P0 | AI 总结可能增加原文没有的数字、条件、因果或结果 | 为数字、阶段、条件和因果词增加支持检查；无法支持时降级为未知 | 总结中的硬事实均能绑定来源；不支持的事实被删除或标记待核实 |
| B06 | P1 | 所有案例都显示“内容性质待核实”，质量信号失去作用 | 结合第一人称、转述词、作者标记和推广信号生成受控分类 | 认证不等于专业、未认证不等于低质；分类理由可查看 |

### C. 结果阶段与证据一致性

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| C01 | P0 | 阶段标签可能是“转专业获批”，结果依据却是“毕业进大厂” | 每个阶段绑定支持该阶段的专属 quote，不复用无关结果片段 | “获批”必须由录取、通过或转入原文支持；就业结果只能支持就业阶段 |
| C02 | P0 | 同一卡片同时显示“阶段达成”和“结果原文不代表目标达成” | 统一阶段状态、标题和解释文案 | 页面只出现一种不矛盾的阶段口径 |
| C03 | P1 | 阶段达成容易被理解为整个选择成功 | 明确“当前最远可验证阶段”，分开展示短期阶段与长期结果 | 转入专业不自动等于适应、毕业或就业成功 |
| C04 | P1 | 路径头部统计阶段达成数量但未说明统计口径 | 增加统计说明与覆盖范围 | 用户能知道统计的是当前样本中的可验证阶段，不是成功率 |

### D. 条件语义、量纲和比较

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| D01 | P0 | GPA 与排名混用，案例“前 15%”可能被解析成绩点 15 | 拆分 `gpa_value`、`gpa_scale`、`rank_percentile`、`rank_position` | 不同量纲不比较；4.0 GPA 不再与前 15%直接比较 |
| D02 | P0 | “大一”和“第 1 学期”被混用 | 拆分年级与学期字段，建立可解释换算但不自动等同 | 大一保持“大一”；只有用户明确学期时才记录第几学期 |
| D03 | P0 | 日投入和周投入可能被 AI 自动换算 | AI 预填只接受值本身出现在用户原句中的结果；换算仅作为可选计算值 | “每天 2 小时”不会自动写成“每周 14 小时” |
| D04 | P1 | 数值字段缺少单位和口径 | 输入旁持续展示小时/天、小时/周、元/月、分制等单位 | 编辑前后单位不丢失；提交值和显示值一致 |
| D05 | P1 | 条件相似/不同只按数值，缺少量纲和语义兼容检查 | 比较前先检查字段 ID、单位、尺度与语义类型 | 只有同字段、同单位、可比较的双方值才生成相似或不同 |
| D06 | P1 | 顶部条件标签只展示“是、1学期、4.0”等值 | 使用条件词典标签展示“条件名：值” | 任一标签脱离上下文仍可理解 |
| D07 | P1 | “不知道/尚未核实”在数值框里看起来只是空白 | 增加明确的未知状态和撤销入口 | 用户能区分未填写、明确不知道和已填写 |

### E. 搜索前访谈与初始化表单

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| E01 | P1 | 初始化表单最多 6 项，可能遗漏用户已经明确说出的重要信息 | 已识别条件不占追问名额；追问数量单独设上限 | 用户已明确提供且通过原文校验的相关条件均被保留 |
| E02 | P0 | 学校、目标专业、目标岗位等关键条件为空时仍可搜索并给出判断 | 为每种决策类型定义必需条件、推荐条件和可选条件 | 必需条件缺失时只能搜索通用经验，不能输出个性化可行性结论 |
| E03 | P1 | 内部术语“学业路径原子条件”等直接展示 | 将内部 group 映射为用户语言 | 页面不暴露算法内部命名 |
| E04 | P1 | `single_choice` 被统一渲染为是/否，无法表示年级、考核方式等 | 条件词典为选择型字段提供受控选项数组 | 每种选择控件与字段语义一致 |
| E05 | P1 | `multi_choice` 与前端 `multi_select` 命名不一致 | 统一字段类型枚举并增加契约测试 | 词典中每种 answer type 均有对应控件和测试 |
| E06 | P1 | 用户的背景、时间和限制可能留在整段原话中，没有进入结构化条件 | AI 提取带原句的候选值，本地校验后写入初始化表单 | 原话信息不丢失；无法核对的值保持空白 |
| E07 | P1 | DeepSeek 不可用时搜索前流程完全中断 | 使用本地范围识别和基础字段目录作为降级表单 | 模型失败时仍可完成初始化并明确提示降级状态 |
| E08 | P2 | 240 字限制不足以描述复杂背景 | 放宽输入长度，并将搜索关键词与完整用户画像分离 | 长描述可保存，但搜索查询保持短且聚焦 |

### F. 搜索策略与来源覆盖

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| F01 | P0 | 分析主要基于搜索摘要，摘要可能截断否定词和结果上下文 | 在授权和接口允许范围内读取更完整内容；否则明确标注“仅摘要”并降低结论等级 | 摘要不能支持的结论不进入最终判断 |
| F02 | P1 | 完整用户描述直接拼入搜索词，容易产生噪声 | 由决策类型、目标、硬条件生成短查询，完整画像只用于分析 | 每轮查询可解释且长度受控 |
| F03 | P1 | 五轮搜索缺少提前停止条件 | 根据来源数量、路径覆盖、正反覆盖和新增率决定是否继续 | 信息充足时少用额度；信息不足时仍不超过五轮 |
| F04 | P1 | 41 个来源只形成 4 个案例，但页面不解释其余来源 | 记录未采用原因：重复、无行动、无结果、路径不符、引用无效等 | 结果页展示采用数、未采用数和主要原因 |
| F05 | P0 | 学校规则具有年份和机构约束，通用知乎经验可能制造错误可行性印象 | 将官方规则设为资格判断的独立前置层 | 无学校当年规则时明确显示“资格未知”，不由知乎经验替代 |
| F06 | P1 | 旧政策与新政策可能混入同一批来源 | 增加时间识别、政策年份过滤和过期提示 | 政策性结论必须显示年份；过期材料不支持当前资格 |
| F07 | P2 | 只按知乎链接去重，可能保留同一故事的转载或重复摘要 | 增加标题、作者、核心片段的近重复检测 | 重复故事不被当成多个独立样本 |

### G. 搜索后动态条件表单

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| G01 | P0 | 补问上限过低：模型最多 2 个，单条件规则上限 1 个，最终可能只有一个或没有 | 分开“候选条件池”和“本轮展示上限”，按价值排序展示 6–10 项 | 有足够缺失条件时不再只出现一个问题 |
| G02 | P0 | 只允许来源触发问题，基础画像明显缺失时可能显示“没有新问题” | 分成证据触发问题和基础缺失问题两组 | 两类问题来源清楚，基础缺失不会被误报为信息完整 |
| G03 | P1 | 动态问题目前位于侧栏，不足以提示用户 | 分析完成后弹出独立动态条件界面，同时允许稍后处理 | 新问题醒目但不阻断用户查看已有结果 |
| G04 | P1 | 逐题回答会重复刷新结果 | 所有动态条件集中提交，只执行一次本地重新匹配 | 一次提交、零知乎搜索、零 DeepSeek 调用 |
| G05 | P1 | 动态问题没有完整呈现触发来源和影响 | 每题显示提问原因、触发案例、原文和影响范围 | 用户能判断为什么要回答以及不回答的后果 |
| G06 | P1 | 初始化条件和搜索后新增条件没有版本边界 | 保存初始版本、动态版本、修改时间和来源类型 | 能比较哪些条件由用户初始提供，哪些由新经历触发 |

### H. 结果页信息架构与交互

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| H01 | P1 | 页面很长，每个案例重复大量条件对照 | 先展示路径结论和关键差异，案例与逐项对照按需展开 | 首屏可看清路径、做法、风险和待确认条件 |
| H02 | P1 | 缺少“覆盖了什么、没有覆盖什么”的研究摘要 | 增加覆盖范围、来源数量、案例入选和空白区域摘要 | 用户不会把未搜索到误解为不存在 |
| H03 | P1 | 成功、受挫、未知数量容易被理解成比例 | 将筛选数量标记为当前样本计数并增加禁止推断成功率说明 | 页面不使用“成功率”或近似表达 |
| H04 | P2 | 浏览器历史和服务端研究记录职责不清 | 统一命名并说明“当前设备快速历史”和“持久研究版本” | 用户知道记录保存在哪里、如何删除 |
| H05 | P2 | 修改条件入口和动态补问入口存在功能重叠 | 明确“编辑全部条件”和“回答新证据问题”两个动作 | 两个入口目的、影响和调用成本清晰 |
| H06 | P2 | 缺少桌面、移动端完整流程的视觉回归 | 增加关键视口截图和文本溢出检查 | 首页、表单、路径页、弹窗在桌面和移动端均无重叠或截断 |

### I. 隐私、安全与公网运行

| ID | 优先级 | 问题与影响 | 改动方向 | 验收标准 |
| --- | --- | --- | --- | --- |
| I01 | P0 | 首页未明确说明用户描述会发送给 DeepSeek，并用于生成知乎搜索查询 | 首次提交前展示简短数据用途和外部服务说明 | 用户提交前知道发送内容、目的和保存范围 |
| I02 | P0 | 用量记录实际保存问题前 120 字，与“指标不记录用户问题”的说明冲突 | 默认不记录问题原文，改存不可逆摘要或明确取得同意 | 文档、代码和实际存储一致 |
| I03 | P0 | 公网环境下开发者密钥入口和写入接口没有管理员认证 | 生产环境隐藏入口，并为密钥接口增加管理员认证或完全禁用 | 未授权访问者无法查看状态或修改全局密钥 |
| I04 | P0 | 搜索和分析接口没有用户限流与预算控制 | 增加 IP/会话频率限制、并发限制、每日预算和熔断 | 单一访问者不能快速耗尽知乎或 DeepSeek 额度 |
| I05 | P0 | 先前在聊天中出现的密钥不能继续用于公网 | 部署前轮换知乎和 DeepSeek 密钥 | 线上只使用新密钥，旧密钥已撤销 |
| I06 | P1 | 本地 JSONL 缺少并发、备份、保留期和用户级删除能力 | 公网版本迁移到数据库或 KV，并制定保留与删除策略 | 多实例和并发写入可靠，用户数据可按规则删除 |
| I07 | P1 | 管理后台存在默认密码 | 生产启动时强制检查 `ADMIN_PASSWORD`，缺失则拒绝启动管理功能 | 公网不存在默认管理密码 |

> 名词确认：上一轮语音中的“获取孕妇的信息”暂按“获取用户的信息”处理；如确实指孕妇这一特定场景，应另立需求，不并入当前转专业 / 转行业画像。

## 四、修正计划

### 阶段 0：冻结错误输出（P0，先完成）

1. 修复 GPA/排名、年级/学期、日/周投入的字段和量纲。
2. 修复阶段标签与结果引用的一致性。
3. 禁止态度、建议和无行为片段进入“采取的行动”。
4. 无官方规则时，将校内转专业资格明确降级为未知。
5. 增加针对当前真实错误样本的回归测试。

完成条件：现有“4.0 vs 前15%”“1学期 vs 大一”“获批 vs 进大厂”三个错误均不能再次出现。

### 阶段 1：重建总结与证据层（P0）

1. 调整模型输出结构，分开 summary、quote、applicability 和 limitation。
2. 恢复亲历、转述、建议、推广分类。
3. 默认显示 AI 总结，原文折叠展示。
4. 为总结中的数字、阶段和条件建立引用检查。

完成条件：背景、行动、结果、做法和风险均有真正的归纳内容，且硬事实可回到原文。

### 阶段 2：拆分转专业行动分枝（P0/P1）

1. 分离决策类型和行动分枝字段。
2. 建立校内转专业受控行动分枝及归类规则。
3. 支持主分枝和次要行动标签。
4. 更新路径导航、统计和空路径说明。

完成条件：同一“校内转专业”研究至少能按来源中的实际行动形成多个分枝，而不是固定一个节点。

### 阶段 3：完善两阶段条件表单（P0/P1）

1. 重构搜索前必需、推荐和可选条件。
2. 统一字段类型、选项、单位、未知状态和用户语言标签。
3. 搜索后建立证据触发与基础缺失两个问题池。
4. 分析完成后弹出 6–10 项动态条件表单，支持稍后处理和集中提交。
5. 保存初始化和动态条件版本。

完成条件：关键条件缺失时不会输出伪个性化结论；动态表单不再只显示一个问题。

### 阶段 4：优化搜索、来源和结果页（P1/P2）

1. 将搜索查询与完整用户画像分离。
2. 增加搜索提前停止、时间过滤和近重复检测。
3. 展示来源采用/淘汰原因与覆盖范围。
4. 重排结果页层级，减少重复对照并加强空白说明。
5. 统一历史记录入口和命名。

完成条件：用户能在首屏回答“有哪些走法、哪些做法可参考、哪些风险相关、还缺什么信息”。

### 阶段 5：公网安全与发布验收（P0/P1）

1. 增加数据用途提示和隐私说明。
2. 移除用量日志中的问题原文。
3. 禁用或保护生产密钥入口，强制管理密码。
4. 增加限流、预算、熔断和监控。
5. 轮换全部已暴露密钥，迁移持久数据存储。
6. 完成桌面、移动端、失败恢复和并发测试。

完成条件：通过安全检查后才允许完全公开；此前只用于受控开发者试用。

## 五、测试与验收矩阵

| 范围 | 必测场景 |
| --- | --- |
| 量纲 | 4/5 分制 GPA、百分制、前 15%、年级名次、大一、第 1 学期、每日/每周投入 |
| 阶段 | 申请意向、提交申请、通过考核、获批转入、开始上课、适应、毕业、就业 |
| 内容性质 | 第一人称亲历、身边人转述、一般建议、机构推广、只有观点没有行动 |
| 路径 | 校内转专业六类行动分枝、跨专业读研、辅修、第二学士、无法归类 |
| 表单 | 信息充分、信息很少、矛盾输入、明确不知道、修改决策类型、模型不可用 |
| 搜索 | 少结果、重复结果、过期政策、只有单侧经验、摘要截断、五轮提前停止 |
| 动态补问 | 0、1、多个证据问题；多个基础缺失项；集中提交；稍后处理 |
| 隐私安全 | 未授权密钥修改、默认管理密码、频率攻击、日志原文、数据删除和密钥轮换 |

## 六、进度更新规则

1. 每个问题状态使用“待解决 / 进行中 / 待验收 / 已解决 / 暂缓”。
2. 开始改动时在本文件追加实施记录；没有测试或真实样本证据时不得标记已解决。
3. 每个阶段完成后运行全量测试、代码检查和生产构建。
4. 影响结论的改动必须增加固定回归样本，不能只靠页面人工观察。
5. 公网安全项未完成前，发布状态保持“受控开发者试用”。

## 七、当前状态

### 按实施难度排序

以下为实施顺序；同组内先处理会误导用户判断的问题。每项独立验证、提交并同步 GitHub，未经验证不标记完成。

| 顺序 | 难度 | 问题编号 | 工作内容 |
| --- | --- | --- | --- |
| 1 | 小 | D06、E03、C02、C04、I01 | 条件名称、用户语言、阶段文案、统计口径与数据用途 |
| 2 | 小至中 | D04、D07、E08、G05、H05 | 单位、未知状态、输入长度、补问依据、入口含义 |
| 3 | 中 | I02、I03、I07、E07、E04、E05、A04 | 日志与管理入口、初始化降级、字段选项、类型修正 |
| 4 | 中 | D01、D02、D03、D05、C01、C03、F05、F06 | 数值量纲、结果阶段、官方资格与时效 |
| 5 | 中至大 | E01、E02、E06、G01、G02、G03、G04、G06 | 两阶段表单、完整条件保留、动态补问与版本 |
| 6 | 中至大 | F01、F02、F03、F04、F07、H01、H02、H03、H04 | 检索上下文、覆盖与停止、去重、结果信息架构 |
| 7 | 大 | A01、A02、A03、B01、B02、B03、B04、B05、B06 | 行动分枝、总结与证据语义、内容分类 |
| 8 | 大 / 外部依赖 | I04、I05、I06、H06 | 预算限流、密钥轮换、数据存储、完整视觉验收 |

已具备实现的项目仍需按本文件的完整验收标准复核。此前核查中的“部分实现”不等于全部完成；密钥撤销状态需要平台侧证据。

### 单项实施记录

补充 E06 验收案例：虚构输入“我在职，想转行做开发”曾被模型预填为“当前岗位职能：在职”。连续引文存在不能证明字段语义正确，后续须增加就业状态与岗位职能的区分校验。

E01/E06 后续复核：已移除模型候选及 `conditionAnswers` 的前 12 项静默截断，所有候选仍须逐项校验，所有提交的词典条件均保留并校验。已验证第 13 项有效候选不会因前面的错误候选而丢失、15 项条件提交完整保留、第 12 项之后的非法字段/类型会明确报错；全量 136 项测试、代码检查和构建通过。此项修复关闭下表 E06 所列的候选/结构化画像截断遗留项；动态 `answers`/`skipped` 的历史上限继续由 G01/G06 跟踪。

E02 后续复核：修复条件重算沿用旧 researchMode/适用性的问题，每次重新计算必需条件；未知路径或尚未核实条件不进入个人模式。清空必需字段后恢复通用参考，切换路径排除明显不符的旧案例。162 项测试、代码检查与构建通过。

C01/C03 后续复核：正向阶段明确排除未通过/未获批/未入职等否定；逐候选先验证再排序，避免第一个无效片段阻断后续有效阶段；优先使用本地已验证的最远阶段，不让模型较早阶段遮蔽后续结果。新增未通过考核及计划/实际录用混合片段回归；164 项测试、代码检查与构建通过。复杂跨年份主体和结果演化仍需人工核查。

G01/G06 后续复核：移除动态 answers/skipped 的12项静默截断，逐项校验并对跳过问题去重；非法格式明确报错。20项答案及18项跳过问题完整保留，165 项测试、代码检查与构建通过。此项关闭上文所列动态历史截断遗留项。

D01/D03/D05 后续复核：用户侧解析同样拒绝日周跨字段；后百分位/倒数名次不再被解释为前百分位/正向名次，百分比不作为具体名次。GPA 分制支持“4分制”等后缀。166 项测试、代码检查与构建通过；未支持的排名方向保持未知，不自动换算。

| ID | 状态 | 改动与验收 |
| --- | --- | --- |
| I05 | 待平台侧操作 | 聊天中曾出现的知乎/DeepSeek 密钥尚无撤销证据，本轮没有自动轮换、没有上传任何密钥；真实审核测试仅在本机读取现有配置。正式公网部署前必须由持有人在平台生成新密钥并撤销旧值，不能仅修改本地配置就标完成。 |
| I06 | 部分解决 | 生产研究版本迁入独立 SQLite，事务保存、按会话隔离读取/删除/复用；Secure/HttpOnly/SameSite Cookie 绑定匿名会话，保留30天、最多60版/8MiB，旧开发 JSON 不迁移不删除。跨会话访问与重算拒绝、双连接保存、落盘与删除测试通过；161 项测试、代码检查与构建通过。必须 HTTPS/持久卷；跨主机存储、异地备份与恢复验证、反馈/缓存/日志级全量删除仍未完成，不能宣称全部数据可用户级清除。 |
| I04 | 已实现单机持久预算 | 生产启用连接频率/每日连接预算和全局每日预算，执行前按最坏调用预留；重算不消耗外部预算，失败不退款。SQLite 事务支持同文件多进程预留、重启持续和 UTC 次日恢复；不信任 X-Forwarded-For。全站默认 50 搜索/100 模型，单连接 10 搜索/20 模型，6 请求/分钟；保留现有并发限制。159 项测试、代码检查与构建通过。需 Node 内置 SQLite 与持久卷；跨主机不共享预算、代理用户可能共用连接限制，不宣称已具备分布式公网保护。 |
| B06 | 已实现受控信号分类 | 在 B04 自述/转述/建议/未知基础上，增加明确招生、购买或付费联系用语的推广信号，保留触发原句供展开核查；不按认证、机构身份判断，劝阻/否定推广语句不触发。含行动的推广内容保留而非直接删除。158 项测试、代码检查与构建通过，含认证不触发和反推广语句回归；复杂隐性推广仍可能漏识别。 |
| B05 | 已增加双层支持检查 | 总结先检查新增数字、学历、日/周口径、就业/获批/毕业里程碑及明确否定，再进入批量模型语义复核；本地拒绝项即使模型愿意通过也不发送审核、不展示为总结。新增项目≠就业、未就业≠就业、学历猜测、日周混淆测试，157 项测试、代码检查与构建通过。复杂数值单位、隐含因果/主体仍依赖模型复核，不能保证全部自然语言推断正确；保留原句及人工核查边界。 |
| B04 | 已实现基础性质区分 | 行动引文按自述线索、明确转述、一般建议、主体未知分类并显示理由；建议不作为亲历案例，第一人称也明确不等于真实性验证。模型 kind/作者认证不直接控制分类。156 项测试、代码检查与构建通过。混合表达仍保守，推广信号由 B06 单独处理。 |
| B03 | 已实现保守行动筛选 | 行动引文需包含可观察的学习、练习、申请、投递、项目制作等行为；仅态度、决定、愿望、建议或明确未实施的句子不纳入案例，保留来源及行动待核实原因。新增纯态度整条分析剔除和正反行为样例；修正规则后现有三条历史学习样本仍通过。155 项测试、代码检查与构建通过。复杂否定、转述、混合句仍不能保证完全识别，B04/B06 补充内容性质。 |
| B02 | 已实现，待视觉验收 | 做法/风险归纳加入同一次批量证据复核，不再额外增加调用；通过后保留归纳和原句，未通过仍明确展示原片段。已复核行动总结可复用于做法摘要。适用性保留本地双方条件对照，但移除“条件不同就证明风险相关”等过度结论；通用模式不输出个人适用判断。153 项测试、代码检查与构建通过，含风险归纳保留原句回归。模型审核并非真实世界事实核验。 |
| B01 | 已实现，待视觉验收 | 背景/行动/结果保留独立总结与原句，先做数字/因果初筛，再用一次批量模型复核判断支持关系；唯一明确通过才展示总结，否则保留原句并标示未复核。重复/异常审核不放行，复核失败不抹掉已有案例。分析预算改为最多 2 次，复核 usage 合并，动态重算仍 0 次。152 项测试与代码检查通过、构建通过；真实 DeepSeek 虚构片段测试通过：允许“练习并完成项目”总结，拒绝“已成功入职”，使用 297 tokens。复核不保证事实真实性，B05 继续强化本地硬事实约束。 |
| A03 | 已实现，待视觉验收 | 每个案例只归入一个主分枝，保留所有匹配行动标签和各自关联的行动引文；页面说明主分枝只是导航归类。转行分枝也改为只用行动引文，不因标题或无关背景推断准备方式。新增多行动单案例不复制、标签证据和输入不变性测试；150 项测试、代码检查与构建通过。 |
| A02 | 已实现受控类别 | 转专业增加成绩与资格核对、先修补齐、考核准备、申请材料准备、转入后适应、调整替代方案六类；仅依据行动引文，明确否定/计划/建议句不分类，无法归类进入待确认。149 项测试、代码检查与构建通过。规则仍偏保守，不宣称覆盖所有自然语言行为。 |
| A01 | 已实现，待视觉验收 | 路径对象分离 decisionRoute 与 actionBranch：同一校内转专业选择可按已校验行动引文分为先修补齐、考核准备、申请材料及待确认，不再固定只有“校内转专业”一个分枝。新增同决策多分枝、案例不复制测试；148 项测试、代码检查和构建通过。更多受控行动类别由 A02 扩充，语义真实性由 B03 继续强化。 |
| H04 | 已实现，待视觉验收 | 命名统一为“当前设备快速历史”和“服务端研究版本”，保存按钮与隐私说明同步；服务端入口明确保存在运行网站的服务器而非访问者电脑，删除提示说明两类记录互不联动，并保留未隔离访问者的风险提示。代码检查与构建通过。隔离和删除范围实际行为仍由 I06 处理。 |
| H03 | 已实现，待视觉验收 | 已有筛选区明确说明当前路径样本数、每段计一次、不能推算成功率；本轮补齐筛选组及计数的无障碍标签，保留阶段而非整体成功的名称。统计无比例计算，H02 同步显示覆盖缺口。视觉复核待浏览器服务恢复。 |
| H02 | 已实现，待视觉验收 | 结果顶部新增覆盖摘要：来源数、重复数、入选案例数、行动路径数；指出缺少正向/受挫阶段、多路径或结果仍未知，不推导成功率或替代路径存在性。首次分析、重算、无来源均生成覆盖信息；无来源保留基础补问。147 项测试、代码检查与构建通过。 |
| H01 | 部分解决 | 案例默认只突出最多两项已核对的条件差异，逐项条件表改为按需展开，保留所有对照与引文。146 项测试、代码检查和构建通过。路径结论优先已有基础布局，但首屏信息密度/移动端仍需视觉验收，完整摘要质量由 B01/B02 解决。 |
| F07 | 已实现保守去重 | 保留所有有效链接，120 字以上规范化相同片段标记 duplicateOf；近重复要求同作者、同标题、四字片段相似度至少 98%，且数值/否定/结果关键词签名相同。重复项不发送详细分析，也不允许模型重新纳入独立案例；来源去向显示对应来源。短通用句不合并，数字和否定变化不合并。146 项测试、代码检查与构建通过。无法识别大幅改写转载，不能据此保证作者身份或样本完全独立。 |
| F04 | 部分解决 | 分析新增逐来源采用状态与原因，来源列表展示采用/未采用数量以及各条去向。已准确区分路径不符、行动引文失败、已采用；未入选时明确模型选择/数量限制可能性及原因未知，不捏造“无价值/无结果”。新增引文失败与未入选去向测试；145 项测试、代码检查与构建通过。无行动/无结果/推广等深层分类仍依赖 B06 的语义审核，近重复来源由 F07 处理。 |
| F03 | 部分解决 | 新增检索停滞规则：至少完成三轮，连续两轮累计有效去重来源数不变则停止；否则最多五轮。记录并展示停止原因，明确不代表信息完整；空结果也不无限重复。新增增长、连续停滞、空结果、最大轮数测试，144 项测试、代码检查与构建通过。尚未实现基于已验证行动路径/正反结果覆盖的“足够即停”，不能用摘要关键词冒充语义覆盖；真实接口节省效果待验。 |
| F02 | 已实现，待视觉验收 | 每轮保留实际查询、查询目的、返回数和累计去重来源数，结果页提供查询记录，包括零结果轮次。查询仍只用路径和受控核心字段，不拼完整叙述；排除未知占位值，并修复自适应第二轮直接拼入过长目标字段的问题。查询记录随研究保存，指标日志仍由白名单排除 query。新增各查询变体长目标/未知值测试；143 项测试、代码检查与构建通过。 |
| F01 | 部分解决 | 来源及事实增加 search_excerpt 依据标记；案例与来源列表就近显示仅摘要、未核验全文及真实性、可能缺失否定/转折/后续结果。事实引文不再允许仅命中标题，必须出现在返回片段。新增标题伪证据回归，142 项测试、代码检查和构建通过。尚未取得/实现授权全文读取，摘要内部语义与截断识别仍由 B03/B04 跟踪，不把提示文案当作完整语义保障。 |
| G06 | 已实现，待视觉验收 | 每次研究保存条件版本、时间、完整结构化值及变更前后值；按 initial/basic/evidence/edit 标明来源，经历触发项保留来源和原句。旧记录首次重算建立 legacy 基线，不捏造过去修改历史；无值变化不新增版本。结果侧栏可展开修改记录。新增来源分类、不覆盖旧版、清空值、旧版兼容、落盘回读测试；141 项测试、代码检查与构建通过。自由补充 answers/skipped 的历史条数限制仍需另行收口，不计为结构化条件变更；视觉验收待浏览器服务恢复。 |
| G04 | 已验证后端，待交互验收 | 现有 DynamicConditionForm 已集中收集多题，只在表单提交时调用一次 explore；沿用 activeRequest 防重复提交。新增内置历史样本 API 回归测试：一次提交时间/现岗位/目标岗位等条件，来源完整复用，searchCalls=0、分析 provider=local，分析阶段仅 1 次。139 项测试通过；与 G03 共用批量表单，浏览器交互验收仍待服务恢复。 |
| G03 | 已实现，待交互验收 | 分析完成且存在补问时打开原生独立 dialog；支持稍后处理/Escape，侧栏展示剩余数量并提供重开入口；关闭不卸载当前表单，保留本轮草稿。经历入口关闭弹窗并按 sourceId 找到案例再跳转，修复原 sourceId/caseId 混用。138 项测试、代码检查和构建通过，本地 4318 页面 HTTP 200；浏览器控制服务不可用，焦点、滚动和窄屏视觉验收未完成。 |
| G02 | 已实现，待视觉验收 | 服务端增加基础必需条件补问，与案例证据问题区分为 basic/evidence；基础问题优先，不伪造引文或来源，界面分别标明类别，基础项不显示原文/经历入口。未知、尚未核实不计为已知基础条件；已跳过的不重复询问。首次分析与重算共用生成逻辑。138 项测试、代码检查和构建通过；浏览器控制服务启动失败，视觉验收暂缺。 |
| G01 | 已解决 | 删除条件候选生成阶段的两题提前返回；汇总全部已校验条件证据，按条件 ID 去重，资格/截止时间/投入/收入相关问题优先，每轮最多展示 8 项。首次分析与本地重新匹配共用选择函数，不再在重新匹配后截为两题；同步模型提示。新增 12 候选选 8、6 候选全部保留、重复来源去重、已回答/跳过排除、空候选不凑数测试；全量 137 项测试、代码检查与构建通过。G02 基础缺失问题、G03 独立界面、G04 批量提交仍未完成，本轮未进行浏览器视觉验收。 |
| E06 | 部分解决 | 自动填入增加字段语义防线：拒绝把在职/待业等状态填为岗位职能；当前学历要求个人背景原句，拒绝目标要求、计划及他人背景；年级、学期、绩点、排名、日/周时间候选必须通过各自解析器并与原句数值一致；未知值保持空白。新增模型错误候选与真实输入保留测试，全量 134 项测试、代码检查和构建通过。本轮使用虚构输入与模拟模型返回，未消耗远程额度。剩余：其他自由文本字段的主体/意图/否定校验，以及超过 12 个候选及画像存储截断问题；未宣称完整语义验证。 |
| E02 | 已解决 | 五种决策路径已设定服务端必需条件：校内转专业/辅修/二学位需学校、现专业、目标专业；跨专业读研需现专业和目标专业；转行需现岗位与目标岗位。缺失时标记为 `general`，页面明示“通用经验研究”，服务端将三问结论降级为通用参考，不输出针对用户的可行性或适配结论。 |
| E01 | 已解决 | 初始表单已拆分“已识别条件”与“待追问条件”的名额：所有通过原句校验的已知条件全部保留，不占用问题上限；另外最多展示 6 个未填问题。表单提示同步更新。已验证 8 个已知条件全部保留且未填项不超过 6 个；40 条转专业/转行评测用例仍满足范围限制。全量 131 项测试、代码检查和生产构建通过。 |
| F06 | 已解决 | 官方材料年份已纳入资格评估：优先使用用户填写的 `policy_year`，未填时使用当前年份；同类规则有多份材料时，当年材料优先。旧年、其他年份和无年份材料会显示年份状态并降为“待核实/仅作参考”，不支持当前资格的“已满足/未满足”判断。已验证 2024 过期材料与 2026 当年材料并存时的选择；全量 130 项测试、代码检查和生产构建通过。 |
| F05 | 已解决 | 官方资格依据已设为转专业结果页的固定独立前置层，与知乎个人经验分开。无 `policy_link`、官方材料读取失败或未识别出资格规则时，明确显示“资格未知”及原因；只有学校名称不会触发官方读取。已验证空官方来源、独立依据标记和实际官方规则识别；全量 128 项测试、代码检查和生产构建通过。 |
| C03 | 已解决 | 案例卡片已统一使用“当前最远可验证阶段”，原“阶段目标达成/受挫”改为“正向/受挫阶段已核对”，路径统计和结果筛选同步更新。服务端按每个转专业/转行里程碑生成边界说明：获批不等于已转入、适应、毕业或就业；已入职不等于通过试用期或长期适配。全量 126 项测试、代码检查和生产构建通过。 |
| C01 | 已解决 | 各阶段已收紧为专属事件语义并继续绑定各自的连续原文。转专业资格要求明确的转专业申请/报名资格；考核通过要求转专业考核/笔试/面试事件；获批不再接受毕业、就业、入职或大厂结果；入职不再接受课程、培训或项目学习。已验证“转专业后毕业进大厂”、公司面试、开发课程三类误标签及真实获批原文；全量 125 项测试、代码检查和生产构建通过。 |
| D05 | 已解决 | 条件对照现在保存并检查字段 ID、单位、语义类型和口径兼容性，只有全部兼容才生成“条件相同/不同”。GPA 必须双方分制明确且相同；排名必须双方比较群体明确且相同。缺失或不同时降为“尚不可比”并显示原因，不进行隐式换算。已验证 4/5 分制、年级/专业排名口径及比较基础元数；全量 122 项测试、代码检查和生产构建通过。 |
| D03 | 已解决 | AI 提交的日投入/周投入候选值现在必须同时通过“原句频率口径 + 提交数值”校验；仅数字相同不再允许跨字段写入。已验证“每天 2 小时”不会被写为“每周 2/14 小时”，“每周 14 小时”也不会被写入每日投入；系统只保留原文明确给出的口径，不自动换算。全量 121 项测试、代码检查和生产构建通过。 |
| D02 | 已解决 | “当前年级”与“当前学期”已分为 `current_stage` 和 `current_term` 两条独立通道；学期解析器不再接受“大一/大二”，年级已纳入校内转专业的初始表单和条件对照。已验证输入“大一”只预填年级、“第 1 学期”只提取学期，两者不自动等同或互相推导。全量 120 项测试、代码检查和生产构建通过。 |
| D01 | 已解决 | 绩点、排名百分比和具体名次已拆分为 `gpa_value`、`rank_percentile`、`rank_position` 三个独立条件与解析通道；名次字段已加入条件词典及表单单位。“绩点排名年级前 15%”只识别为排名百分比，不再与用户 GPA 4.0 比较；同一来源中的 GPA 3.7 与前 15% 仍可分别提取。全量 118 项测试、代码检查和生产构建通过。 |
| A04 | 已解决 | 初始化表单顶部新增“这次研究的选择类型”；可在校内转专业、跨专业读研、辅修、第二学士和转行/转岗间纠正。切换后由本地规则重建条件表，保留仍适用的已填答案，同步范围/路径/行业及后续知乎检索词，不重新调用 DeepSeek。已验证辅修路径的专属字段与检索词、非法路径及 2000 字边界；全量 116 项测试、代码检查和生产构建通过。 |
| E04 | 已解决 | 8 个单选与 3 个多选字段均建立“内部值 + 用户文案”受控选项；初始化表单直接读取词典选项，布尔字段才使用是/否。多选项已从前端硬编码迁回词典，例如收入连续性显示“需要保持稳定收入/可接受短期中断/可以接受中断”。新增选项完整性、唯一性及接口传递测试；全量 114 项测试、代码检查和生产构建通过。 |
| E05 | 已解决 | 遗留的 `assessment_format.multi_choice` 已统一为 `multi_select`；词典导出唯一的 11 类字段类型契约，前端类型收紧为相同联合类型。新增逐项扫描测试，任何旧名称或未支持类型都会失败；全量 112 项测试、代码检查和生产构建通过。 |
| E07 | 已解决 | DeepSeek 生成初始化表单失败时，自动使用本地规则识别转专业/转行业范围、行动类型和目标行业，并按词典优先级生成 4–6 个基础字段；页面明确提示降级及自动识别可能不完整。新增模型异常、路径识别、字段数量和本地预填回归测试；全量 111 项测试、代码检查和生产构建通过。 |
| I03 | 已解决 | 开发者密钥入口与状态/写入接口仅在“非生产环境 + localhost/127.0.0.1/::1”同时满足时开放；生产部署及开发服务器的公网主机名访问均返回 404，前端依据健康接口隐藏入口。新增本机、公网主机名和生产环境回归测试；全量 110 项测试、代码检查和生产构建通过。重启本地服务后确认本机状态接口仍可用，模拟公网 Host 还会先被 Vite 以 403 拦截。 |
| I07 | 已解决 | 管理密码解析已区分环境：开发环境可使用 `life-branches-dev`，生产环境不再回退默认密码；未配置 `ADMIN_PASSWORD` 时管理接口返回 503 并保持网站其他功能可用。新增生产环境有/无配置及管理接口关闭测试；全量 109 项测试、代码检查和生产构建通过。 |
| I02 | 已解决 | 用量记录不再接收或写入问题原文；读取旧日志时会删除遗留 `question` 字段并重写文件。管理后台已移除探索内容列，README 与首页保存说明同步为实际行为。本地两条旧记录已迁移并确认磁盘中无 `question` 字段；新增落盘回归测试，全量 107 项测试、代码检查和生产构建通过。反馈提交仍单独保存问题概要，后续如需匿名反馈应另立隐私任务。 |
| E08 | 已解决 | 初始描述上限由 240 字放宽至 2000 字；知乎查询不再拼接完整用户原话，只使用决策类型、行动类型和最多四个已确认的核心条件，单个条件值限制为 40 字。新增 2000/2001 字边界测试及私人叙述不进入查询的回归测试；105 项全量测试、代码检查和生产构建通过。F02 仍跟踪查询解释、缺失字段降级和检索质量。 |
| H05 | 已解决 | 动态入口改为“回答新证据问题”，说明只更新相关条件对照；整体入口改为“编辑全部条件”，说明会复用现有来源并重新生成对照。两处均明确不重新搜索、不调用 DeepSeek；已核对服务端复用分支仅执行本地 `rematchAnalysis`，构建与代码检查通过。由于本轮浏览器控制服务不可用，页面可见性以运行服务、构建产物和界面源码三处交叉确认。 |
| G05 | 已解决 | 动态题展示提问原因、原文、相关经历入口和回答/跳过的影响；同一来源多个问题使用独立输入 ID。本地打开未回答的历史补问，确认依据入口与影响说明可见；构建与代码检查通过。 |
| D04 | 已解决 | 数值输入持续显示明确单位，绩点显示分制状态；区间、反馈频率等保留文字及口径。本地表单已核对界面与实际值分别为 2小时/天、10小时/周；构建与代码检查通过。量纲兼容校验仍由 D05 跟踪。 |
| D07 | 已解决 | 所有字段明确展示未知状态并提供重新填写入口；字段状态提示根据当前值实时更新。本地虚构表单已验证“数值输入 → 尚未核实 → 重新填写”；构建与代码检查通过。 |
| I01 | 已解决 | 首次发送前说明 DeepSeek 与知乎的数据用途；展开说明浏览器历史、服务端研究、搜索缓存和现有日志的保存范围。已在本地首页确认提交前可见，构建与代码检查通过。I02 日志最小化与 I06 隔离/删除仍需后续处理。 |
| C04 | 已解决 | 路径导航说明阶段达成的样本范围和每段计一次的口径；结果筛选说明当前路径样本数量不能推算成功率。本地页面已确认两处说明，构建与代码检查通过。 |
| C02 | 已解决 | 有阶段时展示阶段名称及专属引文；其他结果单独折叠，无阶段时显示“结果仍待核实”。本地页面已确认两种状态标题，构建与代码检查通过。阶段推断本身的正确性仍由 C01/C03 跟踪。 |
| E03 | 已解决 | 初始化表单将四类内部条件分组映射为用户语言，保留其他原有中文分组。已核对词典所有分组；本地构建和代码检查通过。 |
| D06 | 已解决 | 顶部条件标签使用词典中文名称；动态回答显示问题名称，自由补充显示“补充条件”。本地旧记录实测显示“当前绩点：4.0”“当前学期：1学期”“所在学校：中国农业大学”；构建与代码检查通过。 |

| 阶段 | 状态 | 备注 |
| --- | --- | --- |
| 阶段 0：冻结错误输出 | 待验收 | 已实现阶段引用、未知条件与量纲防线；复杂语义仍需人工样本验收 |
| 阶段 1：总结与证据层 | 待验收 | 已实现独立模型审核、无支持总结降级、行动及内容类型校验；不代表完整语义验证 |
| 阶段 2：行动分枝 | 待验收 | 已拆分行动分枝、保留多行动标签；待真实样本及视觉验收 |
| 阶段 3：两阶段表单 | 进行中 | 已实现多问题弹窗、批量重算与条件版本，版本纳入自由回答及跳过/恢复操作；自由文本预填主体语义仍待完善 |

条件版本补齐（G06）：自由补充回答和跳过状态进入版本快照，单独修改也会新增版本，并记录问题名称、前后值与触发来源；恢复回答可看到跳过状态被清除。新增无结构化条件变化的回答/跳过/恢复回归；176 项测试、代码检查及构建通过，界面沿用已有变更列表。
| 阶段 4：搜索与结果页 | 进行中 | 已实现轮次追踪、停滞停止、去重及覆盖信息；模型提示与本地摘要引用规则已对齐；授权全文、语义覆盖停止和视觉验收未完成 |
| 阶段 5：公网安全 | 进行中 | 已实现生产额度预算、会话隔离持久化、反馈单独删除及本地快照恢复测试；新缓存不再明文保存查询词；密钥轮换、全量删除、异地备份及部署验收未完成 |

反馈隔离复核（I06）：生产反馈进入当前会话的 SQLite，30天有效、最多100条，并计入请求频率限制；提供删除本会话反馈入口，不能删除其他会话。开发共享反馈拒绝按会话删除，旧 JSONL 不迁移不删除。双会话接口及数据库隔离测试、175 项测试、类型检查、代码检查与构建通过；按钮视觉验收仍受浏览器服务不可用阻塞。匿名用量及备份另按留存策略处理，未宣称一次清除所有副本。

缓存留存复核（I06）：新搜索缓存使用事务 SQLite，查询摘要键及来源字段白名单，文件权限600、24小时有效、最多1000条/64MiB。双连接复用、较旧结果不覆盖新结果、查询词及多余元数据不落盘、过期删除测试通过；174 项测试、代码检查与构建通过。旧 JSONL 仅兼容读取，不再追加，未自动删除现有数据；摘要键不是匿名化，内存与研究版本仍按用途保存查询信息，未宣称全量用户级删除。

持久化恢复复核（I06）：新增仅服务端可调用的一致性快照方法，拒绝覆盖已有文件；数据库与快照权限限制为600。虚构数据验证备份后删除再恢复、会话隔离、30天过期过滤与60版上限；生产历史摘要补齐背景/投入/目标。172 项测试、代码检查与构建通过。README 补充停机恢复及删除记录复活风险；未配置异地备份，未备份或改动真实用户数据，全量删除仍未完成。

重算一致性复核（C01/F04/G04）：修改方向后重新核对来源阶段，失效阶段恢复未知；同步更新来源采用状态，移除被排除来源的摘要，避免旧状态与新覆盖统计冲突。新增跨路径过滤与跨领域阶段回归，168 项测试、代码检查及构建通过。本次不新增远程调用。

模型异常隔离复核（B01/B05）：忽略路径、案例和总结列表中的空值或错误容器；有效案例与总结仍正常核验，审核结果中的空项不再阻断有效批准。新增混合损坏列表回归，170 项测试、代码检查和构建通过；无有效候选不发起审核、不编造总结。

总结量纲复核（B05）：硬事实预检增加数字与单位配对，拒绝小时改年、月改周、投入时间改金额、项目数改学分；空格与月份常见写法兼容。新增正反回归，173 项测试、代码检查与构建通过。保留独立模型审核，未据此宣称完整事实或因果验证。

首次预填主体复核（E06）：数值与当前背景字段的 AI 引文补查所在子句，拒绝同学/朋友/岗位要求/假设内容；本地兜底使用相同排除规则，不再覆盖成错误用户条件，本地多处矛盾数值保持空白。新增五类误填及本人/朋友混合输入回归，171 项测试、代码检查与构建通过。仍是保守的局部语义防线，跨子句主体承接、目标自由文本及复杂否定未宣称全面解决。
