# 人生分枝：原型交付记录

## 本地访问

http://localhost:4317/

代码位于 `prototype/`。启动方式见其中 README。已提供临时公网历史样本展示，见 `临时展示记录.md`；公网不开放实时调用，生产构建也不包含本机知乎调用能力。

## 已实现

- 一次研究一个选择，支持自由输入转行、升学、专业选择、出国等问题。
- 前置条件填写、按行动路径导航、经验详情、阶段结果筛选、引用展开与知乎原文访问。
- 基于检索片段的模型分析后端，逐项校验原文引用。
- 动态补问、跳过、修改条件与复用来源重新分析的接口及交互。
- 搜索限流、额度不足、空结果、无可验证路径、分析失败和来源保留状态。
- 历史样本入口：保留三条路径、三段经历、一项补问的本地整理版；完成模型调用后，优先展示真实模型生成的历史分析（三条路径、两项补问），使用的仍是历史片段和虚构人物条件。
- 桌面与移动尺寸的响应式样式；尚未做截图或实机验收。

## 已验证

- 15 项离线测试通过，覆盖来源去重、引用拒绝、条件证据更新、补问抑制、历史样本接口与请求边界。
- TypeScript 检查和 lint 通过。
- 本地页面返回 200；历史样本接口返回 200。追加验证中确认其已加载模型生成的三条路径与两项补问。
- 构建通过。
- 单独的知乎搜索成功，知乎直答对一段虚构经历的 JSON 提取成功。

## 历史验收记录

首次完整实时测试未通过，原因是知乎搜索当日额度已用尽：实查为总额度 10、已用 10。测试结果不应表述为实时全链路已跑通。

补充的外部模型分析调用最初被自动审批拒绝。用户调整权限并要求继续后，重新检查到直答剩余额度为 1，执行分析成功。此次仅分析已有公开片段和虚构人物条件，没有重新调用已耗尽的搜索接口。结果保存在 prototype/.local/archive.json，原型历史样本入口已加载该结果。

追加的语义检查发现：模型将校内学习推断为时间更充裕，补问中再次涉及已说明的不能中断收入条件，并混合了脱产和贷款两个维度。尽管所有引用通过连续片段匹配，仍不能认定推论或补问质量合格。需要改进证据与结论之间的语义约束、已知条件去重，再以独立样本验证；不能把本次调用成功表述为匹配质量验收通过。

## 2026-09-06 实时验证

- 使用虚构测试人物，在独立本地接口运行，不影响公网展示。先测试在职、每日两小时、不能中断收入；再修改为已离职、每日八小时、有半年生活费，并回答一个补问。
- 两次真实搜索取得 9 条去重来源。首轮生成 3 条路径、3 段经历、1 项补问；第二轮生成 3 条路径、4 段经历、2 项补问。
- 第二轮复用全部来源，未重复搜索，已回答问题未以完全相同文字再次出现。引用检查两轮分别拒绝 4 项和 3 项内容。
- 真实后端流程通过，不等于浏览器交互或分析质量通过。测试数据保存在 `prototype/.local/live-verification-latest.json`，未替换公网历史样本。
- 实查剩余额度：搜索 8 次，直答 0 次。本次消耗两次搜索、两次直答，不继续重试。

## 内容质量未通过

- 将“电子信息工程专业”推断为已有编程基础；把“项目独立完成并部署”的引文用来支撑“成功入行”。连续原文匹配不能阻止这类语义越界。
- 来源没有明确在职状态，却被归入“在职自学”或“在职跨考读研”。
- 第一轮缺乏明确受挫经历，两轮都只产生做法建议，缺少用户相关风险提示，尚未满足正反对照主线。
- 第二轮缺失信息中出现“用户是否愿意编造经验”，不应将不诚信做法作为待匹配的正常条件。
- 同一来源集重分析时路径构成发生变化，尚未验证导航稳定性。补问精确去重通过，不代表语义去重合格。

后续重点是逐结论证据约束、阶段结果区分、行动路径稳定性与正反覆盖验收，而非仅加强提示词。还需验证桌面和移动端实际交互、长内容布局，以及可选 WebMCP 浏览器接口。目前不能宣称匹配准确率已经验证。
