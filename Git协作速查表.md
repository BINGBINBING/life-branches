# Git 协作速查表 — 专供 5 人异地团队（Hackathon 用）

> 目标：保证「仓库永远有一份能跑的版本」+「没人乱改被覆盖」。
> 工具：GitHub + GitHub Desktop（图形界面，不用敲命令行）。
> 大原则：**分工尽量错开文件 / 模块**——文件不重叠，冲突就几乎没有，这比学一堆命令有用一百倍。

---

## 谁是干什么的

| 角色 | 该干什么 |
|---|---|
| **看护人（1 人）** | 建主仓库、合并进 main、让 main 永远"能跑"。一开始就定好是谁。 |
| **其余 4 人** | 各自在小分支上做自己那块，写好的内容通过 Pull Request(PR) 交给看护人审。 |

---

## 第 0 步：先在群里对好这 3 件事（动手前）

1. 选好 **看护人**（Git 最明白的担当；都不明白就抓阄指定一个）。
2. 分清楚 **谁做哪个模块 / 哪个文件**（尽量不重叠，防冲突）。
3. 每人装好 GitHub Desktop（desktop.github.com）并注册/登录自己的 GitHub 账号。

---

## 建仓库 & 接上本机（只有看护人新建）

1. 看护人上 github.com → **+** → **New repository** → 填仓库名 → **不要勾选 README 等**，建空仓库 → Create。
2. 看护人本机打开 GitHub Desktop → 登录 → **Add > Clone repository** → 选这个仓库 → 选本地存放文件夹。
3. 在仓库网页 **Settings → Collaborators** 里，把另外 4 人的 GitHub 账号加进来。
4. 其余 4 人各自：GitHub Desktop → **File > Clone repository** → 选这个仓库 → 本地建一份。

---

## 每位队员的日常动作（记住这"1 开工 + 3 干活"）

**开工前（把别人新东西先拿下来）：**
1. 点 **Fetch origin**，再点 **Pull**。
   → 不先拉别人的，直接开改，容易和最新版本脱节。

**干完一小块就存一版（别憋一大天才提交！）：**
2. 左侧能看到你改过的文件 → 勾选要保存的 → 写一句说明（做了什么）→ **Commit to ...**（在本地存一个版本）。
3. 点 **Push origin**（把版本推到线上备份）。
   → 节奏：每完成一个"能跑的小功能"就 commit + push，别等。

---

## 把自己的东西弄回主线（交给看护人）

1. 你本地自测跑通后，到 github.com 你的分支页 → 点 **Compare & pull request**。
2. 填一句：我加了【功能】，涉及【文件/接口格式】。
3. **让看护人（或另一位队友）看一眼、确认能跑**，再点 Merge。
   → 这是天然"质量闸门"：不随便把坏代码进主线。

---

## 撞上冲突怎么办（一定会遇到，别慌）

当两个人改了**同一个文件的同一处**，合并时 Git 会喊冲突。

做法是**人工读文件选一份**，没有神奇命令：

打开冲突文件会看到标记：
```
<<<<<<< HEAD
这里是合并进来那方已有的代码
=======
这里是你的分支带来的新代码
>>>>>>> 你的分支名
```
你要做的：
1. 判断该留哪边（或两头各留该留的）；
2. 删掉标记行（`<<<<<<<`、`=======`、`>>>>>>>` 那几行）；
3. 保存 → Commit → Push。
4. 正因这事麻烦，前面才反复强调：**分工改不同文件**，文件不重叠就根本遇不到冲突。

---

## 三条救命提醒

- ❌ 不要在 main 上直接大改；✅ 在小分支上做，改完走 PR。
- ❌ 不要一大天才提交；✅ 干完一小块就 commit + push。
- ❌ 卡住了不要憋；✅ 定好规矩：**卡超过 1~2 小时就在群里求救**，新手最容易一人卡死一下午。
